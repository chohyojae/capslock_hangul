# caps-hangul-rs 사용 설명서 (USAGE)

Windows 환경에서 macOS의 Caps Lock 동작과 유사하게 동작하도록 도와주는 한/영 전환 유틸리티입니다.

---

## 🌟 주요 기능

1. **Caps Lock 짧게 누름** ➔ **한/영 전환**
2. **Caps Lock 길게 누름** (400ms 이상) ➔ **Caps Lock 대/소문자 토글**
3. **화면 중앙 안내 HUD (오버레이)**
   - 한/영 전환 시 화면 중앙에 `한` 또는 `A`가 macOS 스타일로 잠깐 나타났다가 사라집니다.
   - Caps Lock 토글 시 `CAPS ON` 또는 `CAPS OFF`로 대소문자 전환 상태를 알려줍니다.
4. **정확한 한/영 상태 연동**
   - Microsoft Teams, 웹 브라우저 등 TSF(Text Services Framework) 및 Chromium 기반 앱에서도 정확한 입력기 상태를 감지하여 
     안내 창에 올바르게 표시합니다.

---

## 📦 구성 파일 및 설치 방법

배포 압축 파일의 압축을 풀면 다음과 같은 파일들이 포함되어 있습니다. **모든 파일이 같은 폴더에 있어야 정상적으로 동작합니다.**

| 파일 이름 | 역할 |
| --- | --- |
| `caps-hangul.exe` | 프로그램 본체 (실행 파일) |
| `caps-hangul-tsf-*.dll` | 입력기 상태 감지용 컴포넌트 (3종) |
| `caps-hangul-reader-*.exe` | 다양한 앱과의 연동을 위한 헬퍼 프로그램 (3종) |
| `install-startup.cmd` | 시작 프로그램 등록 및 자동 실행 스크립트 |
| `uninstall-startup.cmd` | 시작 프로그램 등록 해제 및 종료 스크립트 |

### 🚀 설치 및 자동 실행 설정

1. 다운로드한 배포 폴더를 원하는 고정된 위치(예: `C:\Program Files\caps-hangul` 또는 사용자 문서 폴더 등)에 이동합니다.
2. **`install-startup.cmd`** 파일을 더블 클릭하여 실행합니다.
   - 윈도우 부팅 시 프로그램이 자동으로 실행되도록 등록하고, 동시에 프로그램(`caps-hangul.exe`)을 바로 실행합니다.
   - 이제 Caps Lock 키를 사용하여 한/영 전환을 시작할 수 있습니다!

### 🛑 시작 프로그램 해제 및 종료

- 더 이상 사용하지 않으려면 **`uninstall-startup.cmd`** 파일을 더블 클릭하여 실행합니다.
  - 실행 중인 프로그램을 정상 종료하고, 윈도우 시작 프로그램 등록을 안전하게 삭제합니다.

---

## 💡 유의 사항 및 팁

- 본 프로그램은 백그라운드에서 동작하며, 시스템 트레이(알림 영역)에 아이콘이 표시됩니다.
- 프로그램을 종료하려면 트레이 아이콘을 마우스 우클릭한 뒤 **Exit** 메뉴를 선택하면 됩니다.
- 보안 프로그램이나 일부 환경에 따라 실행 경고가 뜰 수 있으나, 외부 네트워크 통신이 없는 안전한 오픈소스 로컬 유틸리티입니다.
